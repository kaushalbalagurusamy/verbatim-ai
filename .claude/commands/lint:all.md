Run the project linter (`npm run lint -- --fix` or `pnpm lint --fix`).  
Repeat until there are **zero** lint errors, committing after each successful fix.  
Return OK on success or FAIL with error log snippet.

For this Next.js/TypeScript project:

1. Run: `npm run lint -- --fix`
2. Check output for remaining errors
3. If errors exist, analyze and fix them manually
4. Commit fixes with message "fix: resolve lint errors"
5. Repeat until clean
